mysql commands:
To Create new user
CREATE USER 'pradeep'@'localhost' IDENTIFIED VIA mysql_native_password USING '***';
GRANT ALL PRIVILEGES ON *.* TO 'pradeep'@'localhost' 
REQUIRE NONE WITH GRANT OPTION MAX_QUERIES_PER_HOUR 0 MAX_CONNECTIONS_PER_HOUR 0 MAX_UPDATES_PER_HOUR 0 MAX_USER_CONNECTIONS 0;
GRANT ALL PRIVILEGES ON `mysql`.* TO 'pradeep'@'localhost';

Update host:
UPDATE `user` SET `Host` = 'localhost' WHERE `user`.`Host` = '%' AND `user`.`User` = 'pradeep';